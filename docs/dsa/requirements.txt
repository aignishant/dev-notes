# DSA & System Design Bible — Python dependencies
#
# Install with:
#     pip install --user --break-system-packages -r requirements.txt
#
# Or (recommended) inside a virtualenv:
#     python3 -m venv .venv
#     source .venv/bin/activate
#     pip install -r requirements.txt
#
# Tested with Python 3.10+.

mkdocs==1.6.1
mkdocs-material==9.7.6
mkdocs-material-extensions==1.3.1
pymdown-extensions==10.20.1

# Optional: kept installed as a fallback. Not enabled in mkdocs.yml because
# Material's native superfences pattern auto-themes mermaid with light/dark.
mkdocs-mermaid2-plugin==1.2.3

# Build optimizer (HTML/CSS/JS minification on `mkdocs build`).
mkdocs-minify-plugin==0.8.0
